@using TechElevator.Web.Models
@model UserViewModel

@{
    ViewData["Title"] = "Pudding Time";
    //Layout = null;
    //var myData = ViewData["Data"] as UserViewModel;
}

<h2>@Model.UserName</h2>
<h3>@(Model.Password)</h3>

@{ 
    if (Model.IsGoodIdea)
    {
        for (int i = 0; i < Model.Amount; i++)
        {
            <h1>PUDDING TIME!!!</h1>
        }
    }
}

